{% extends "blog/common.html.twig" %}

{% block body %}
    {{ form_start(formArticle)  }}
    
    {{ form_row(formArticle.category, {"attr":{"label": "Cat�gorie de l'article"}}) }}
    {{ form_row(formArticle.name) }}
    {{ form_row(formArticle.image,{"attr":{"placeholder": "Image de l'article"}}) }}
    {{ form_row(formArticle.content, {"attr":{"placeholder": "Contenu de l'article"}}) }}
    
    
	<button type="submit">
    	{% if articleExist %}
    	Sauvegarder modifications
        {% else %}
    	Enregistrer
   		{% endif %}
    </button>
    {{ form_end(formArticle) }}
{% endblock %}